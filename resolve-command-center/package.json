{
  "name": "clackly",
  "version": "0.1.0",
  "private": true,
  "description": "Architecture-validation MVP command palette for DaVinci Resolve.",
  "main": "electron/main/main.js",
  "scripts": {
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"wait-on http://127.0.0.1:5173 && electron . --dev-renderer\"",
    "start": "npm run build && electron .",
    "start:electron": "electron .",
    "build": "vite build",
    "test": "node --test interaction/*.test.js config/*.test.js capability/*.test.js script-runtime/*.test.js script-runtime/providers/*.test.js feature-status/*.test.js feature-ui/*.test.js shortcut/*.test.js command-engine/*.test.js execution-adapter/*.test.js resolve/*.test.js electron/main/*.test.js electron/renderer/*.test.mjs && python -m unittest discover -s resolve -p \"test_*.py\" && python -m unittest discover -s script-runtime -p \"test_*.py\"",
    "workflow:install": "powershell -ExecutionPolicy Bypass -File scripts/install-workflow-plugin.ps1"
  },
  "dependencies": {
    "lucide-react": "^1.27.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.4",
    "concurrently": "^9.1.0",
    "electron": "^43.2.0",
    "vite": "^8.1.5",
    "wait-on": "^8.0.1"
  }
}
